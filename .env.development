# Development environment configuration
# Allows Aspire CLI to run without HTTPS certificate on macOS

# Disable certificate validation for development dashboard
# This allows `aspire run` to start dashboard on HTTP in development
ASPIRE_ALLOW_UNSECURED_TRANSPORT=true

# Disable .NET telemetry
DOTNET_CLI_TELEMETRY_OPTOUT=1
